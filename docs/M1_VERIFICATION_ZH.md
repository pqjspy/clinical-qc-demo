# M1验证记录

日期：2026-09-09。范围：数据/契约，不是模型评测。

实际产物：12条输入、12条独立参考、8个预期分类问题项、6个L3路径、3份虚拟方案、9条规则版本。

已执行：

```bash
.venv/bin/python scripts/validate_m1.py
.venv/bin/python -m unittest discover -s tests -v
```

本次27个测试方法通过。它们包含字段/路径/引用/规则范围、缺失和重复参考、禁止答案进入输入、半开版本区间、合成标识、未知L3、错误算术、来源矛盾、多问题和意外规则冲突等检查。运行期间禁止模型相关导入和网络连接，并验证数据文件哈希未变化。

作者给定算术的复算结果：QC002 27分钟；边界样例30分钟；访视第37天；期末应有库存110；多问题样例的PK间隔27分钟；冲突样例的间隔27分钟（可以计算，但不能确定唯一适用阈值）。

规则冲突恰为独立冲突研究中的A/B两条。正常研究的6条v1规则不冲突；v1在2026-10-01零点终止、v2于同一时点生效，不能两版同时适用。

环境：Python 3.12.14、Pydantic 2.13.5，unittest来自标准库。为本项目创建`.venv`，通过`--system-site-packages`复用已安装的Codex Python运行时Pydantic；不是复用旧项目代码或数据，没有联网安装依赖。它是本机开发环境，尚不是隔离、锁版本的可分发部署包。

其他机器后续可使用独立虚拟环境并从`pyproject.toml`安装依赖；正式可复现依赖锁和启动/部署验证在交付阶段完成。本阶段没有启动服务、调用Qwen、训练模型、提交人工审核或测量F1。

局限：预期标签是作者设计，不是医学专家标注；对原文引文做的是精确存在性检查，不能证明语义蕴含。算术参数取自独立参考数据，不是从输入自动抽取。本批为展示fixtures，不算独立评测。拥有本机权限的人可修改文件，当前不是不可篡改存储。
