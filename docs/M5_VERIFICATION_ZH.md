# M5 本地交付验证记录

日期：2026-09-10。结论：本机已有依赖环境的交付工具、私有备份、隔离恢复及应用生命周期演练已通过；不等于新机器安装、整机断网、Docker部署或医院上线验证。

## 本阶段实际修改

- `scripts/serve_m3.py`在创建应用/初始化数据库之前占用127.0.0.1:8911，重复启动不能先触发数据库恢复逻辑。
- `src/clinical_qc_demo/delivery.py`与`scripts/local_ops.py`提供分项状态检查、SQLite一致性备份、完整性验证和仅新目录恢复。不提供覆盖在线数据库或删除目录的操作。
- `scripts/verify_m5.py`在隔离目录验证恢复、新数据目录、一次真实Qwen推理、应用生命周期重建和持久化；运行报告明确限制。
- 新增正式五部分方案、操作指南和演示稿；纠正旧映射中“全部是计划”和“M4通用模型抽取”的过时表述。
- 只改运维/文档/测试，不改M4冻结的模型提示词、计算、知识库和评测输入/答案；不删除用户记录。

## 检查结果

| 项目 | 实际观察 |
|---|---|
| 现有应用 | 127.0.0.1:8911健康，M4 live_local范围；没有重启该服务 |
| 本机模型 | Ollama 0.32.4；Qwen3-4B Instruct Q4_K_M，摘要与冻结身份一致 |
| 重复原生启动 | 新启动进程退出1，提示端口不可用且未初始化库；原服务仍健康，12张业务表内容未变 |
| 私有备份 | SQLite backup API成功，文件清单562项；包含已发布规则和人工历史，账户匹配验证通过 |
| 隔离恢复 | 文件摘要、库完整性/外键、各表内容摘要和账户核对通过；新目录启动应用后可登录和读历史 |
| 新数据目录 | 不含评测答案，真实分析QC002；不是读取预期答案或旧结果回放 |
| 应用重建后持久化 | 同一Python进程内关闭/重建真实FastAPI生命周期后，业务表及应用定义schema一致 |
| 出站限制演练 | Python socket禁止非回环连接，实际记录的6次连接均为127.0.0.1:11434 |
| 在线保护 | 演练前后在线表内容一致；没有往网页的真实运行列表插入演练任务 |
| 冻结保护 | 17个冻结文件摘要一致，18条评测未重跑/改写 |
| 软件测试 | `python -m unittest discover -s tests -q`：141项通过，含19项M5测试 |

备份时业务状态：3个账户、18条记录、3份规则库快照、9个任务/运行、8条人工复核、2份草稿和2次发布、52条审计、21条提醒事件。当前规则快照为`aa5251e2-b01f-4775-a58d-45c8339cbb08`，包含原始规则及此前用户/验收发布的规则，未回退成M1初始JSON。

M5测试包含WAL已提交数据备份、原库不变、凭证匹配、文件损坏/遗漏/新增、符号链接/越界路径、已有目标/备份内嵌恢复拒绝、活动任务拒绝、相关运行轨迹保存/缺失/不一致、冻结文件改变、应用健康但模型失败、模型摘要不符、端口占用前置退出及SQLite统计元数据的区别。软件测试模型替身不计入真实模型准确率。

## 两次演练全部保留

第一次目录：`runtime/delivery_drills/20260910T093519Z-346f17d1/`。

恢复与真实QC002推理已成功，实际耗时10,225.037ms；随后验证脚本对比整个SQLite schema，误把第二次初始化产生的`sqlite_stat1`统计表当业务结构改变，报告`ok:false`。已在独立小例中复现：只有内部schema摘要变化，业务表内容无变化。没有改写这次失败报告。

修正后，仍核对所有业务表和应用定义的表/索引/触发器，只不把SQLite自有查询统计元数据当成业务变化。增加对应回归测试，显式新开第二次演练，不在脚本内自动重试模型。

第二次目录：`runtime/delivery_drills/20260910T093803Z-3121aeb3/`，`verification.json`记录`ok:true`。

- 实际模型运行：`e047d985-dfff-4e26-9319-7a40c4687307`。
- 输出：`proposed_findings`，L3-PK-001，中风险；代码计算`27.0 < 30 分钟`。
- 耗时：6,362.381ms。不能用单次更快结果替换M4的10.31秒均值或承诺“稳定6秒”。
- 解释引用结构未报错；没有做独立专家语义评估，不能写成“解释100%正确”。
- 模型完整身份与冻结版本一致；业务表、当前规则和17个冻结文件未改变。

使用的私有备份：`runtime/backups/20260910T093741Z-ba0f6536/`。它是演练前时间点的副本；后续完成的文档更新不会倒写进去。更早备份`20260910T093505Z-ece33951`也保留。最终交付文档可在需要时另做新备份，不能把旧快照说成自动同步。

## 没有验证或没有交付什么

- 没有关闭整机Wi-Fi/外网，也没有约束Ollama独立进程的全部网络行为；只能报告本应用Python进程级限制下的本次推理成功。
- 没有在新电脑安装依赖或重新下载模型。恢复演练使用本机现有环境，核对当前源码/静态构建与备份摘要后，用当前源码加载恢复数据；未从备份源码独立启动新Python进程。
- 没有本轮浏览器点击复核/保存验收；此前M3/M4浏览器验证仍见其历史报告。本次未改页面。
- 没有停止/重启在线8911或设置开机自启，未演练运行中强制中断，不自动强杀进程。
- 没有Docker、K8S、vLLM、加密备份、监管电子签名或防篡改存储。没有公开发布或外传数据。
- 没有修复M4的EDC误分或解释语义矛盾，也没有使用这两次交付推理生成新模型成绩。M4首次18条结果保持原样。

依赖仍有Starlette TestClient关于httpx的弃用提示；当前141项测试通过。本阶段不为消除提示擅自升级已锁定依赖，应在新依赖版本下另做回归。
