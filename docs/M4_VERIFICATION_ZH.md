# M4 验证与错误报告 · 2026-09-10

当前版本：六类显式事实执行器 + Qwen分项/解释 + 逐项复核 + 18条冻结合成回归评测。界面和计算能力已扩展，不代表模型在六类都稳定正确。所有材料为合成演示，未使用医院数据。

## 1. 先看真实结果，不只看通过数

冻结于2026-09-10 14:16:40（北京时间）；18条顺序执行完毕，14:20:09保存报告。没有中断、自动重试、替换题或根据结果重新打分。

| 指标 | 首次冻结评测结果 | 应怎样解释 |
|---|---:|---|
| 已尝试 / 计划 | 18 / 18 | 全部留在分母，包括未决和解释失败 |
| 阳性问题召回 | 10 / 12 = 83.3% | 漏了两条EDC问题 |
| L3 Micro-F1 | 0.9091 | TP10、FP0、FN2；Qwen分项与规则共同产生的结果 |
| L3 Macro-F1 | 0.8333 | 固定六类平均；EDC的F1为0 |
| 分项结果完整一致 | 15 / 18 | 只看问题组、状态、L3、风险等，不保证解释完成 |
| 预定义完整工作流结果一致 | 13 / 18 = 72.2% | 再检查总状态、优先级及解释流程；仍非语义人工评分 |
| 多问题分项完整一致 | 2 / 3 | E017虽两项找对，但解释失败，不算完整工作流正确 |
| 高风险漏检 / 低估 | 0 / 4；0 / 4 | 只有4个合成高风险问题，不能推断真实安全性 |
| 应优先路由召回 | 9 / 9 | 仅程序路由标志，不等于网页队列已投递 |
| 不必要优先路由 | 3 / 9 = 33.3% | 对原本常规的记录因误分/解释错误增加处理负担 |
| 硬失败 / 解释失败 | 0 / 18；2 / 18 | 解释失败不抹除程序已找到的问题 |
| 输出证据结构有效 | 134 / 134 | 程序提供的引文位置/规则来源有效；不是Qwen解释引用全部通过 |
| 平均本机耗时 | 10.31秒/条 | 单次顺序小样本，包含快速未决，不是p95或线上SLA |

各类阳性支持数：PK3、访视1、AE2、用药2、授权2、EDC2。前五类本轮各F1=1，EDC=0；尤其不能把只有1条访视得分100%说成该类已验证成熟。

## 2. 五条不完全一致，分别哪里错了？

| 记录 | 实际问题 | 对结果的影响 |
|---|---|---|
| E006 | 把EDC事实中的访视名称片段另分为visit问题，EDC项失去visit_label | 原本可比较的脉搏差异没检出，产生额外未决项 |
| E008 | 程序正确判断AE已有对应登记，但解释用了不存在的`[I-1-R1]`；文字还自相矛盾 | 引用校验拦截，保留程序结果，整条显示部分完成 |
| E009 | 把授权记录中的“采血”等内容误分为pk，授权组事实被拆散 | 原本在授权起点的正常操作被转成未决，而非正确完成本项检查 |
| E012 | 用药缺字段被识别；EDC部分的主体/访视被拆成visit，EDC缺subject_id | 漏了第二项EDC差异，多问题不完整 |
| E017 | PK与AE两项正确；解释生成了`[I1-R1-F2]`等不存在的引文 | 两项分类仍计对，但解释失败，完整工作流计错 |

这些错误已经用于本报告解释，因此该18条以后不是“从没看过的测试”。本轮代码、提示词、输入、参考与分数保持冻结；未来改进应单独标新版本，不能修完后覆盖首次成绩。

开发用12条也保留了所有运行。最终提示词v7的开发运行中，`DEMO-ROLE-01`仍被误分为PK；`DEMO-MISSING-01`被拆成两个未决问题而非理想的一项。未知/冲突/歧义均未被当作正常。开发展示不进入上面的18条分数。

## 3. 模型、数据与冻结证据

- 模型：本机 `qwen3:4b-instruct-2507-q4_K_M`，4B、Q4_K_M；Ollama 0.32.4。
- digest：`0edcdef34593eac1aa2be9c7d06c432dcf81945adca5eca2f27662c18f168ba0`。
- 参数：temperature=0、seed=42、num_ctx=12288、num_predict=2600；提示词版本 `m4-clause-routing-v7`。
- 冻结清单：`data/evaluation/m4_v1/manifest.json`；输入、参考、规则、推理/评分程序、模型与参数均记录。完成后再次只读核对，冻结文件摘要未变。
- 首次结果：`runtime/evaluations/m4-v1/report.json`，每条完整结果另存同目录，原始模型请求/响应也在各run目录。
- 报告的manifest摘要：`9bc07b6a9ea182a592024c5385f0fb3e1342485c81fed75a7698e5b898d4a536`。
- 清单及启动标记采用排他创建；现有本轮结果不能由脚本覆盖。不要删除标记来重跑。本地管理员能改变文件，不能把hash称为监管级防篡改。

18条输入与12条展示不同，但共享作者、虚拟规则和有限事实语法，部分是边界/重组变体；不是严格模板隔离的独立泛化测试。参考由作者给出，不是医学专家独立标注，也不是待评Qwen生成的答案。逐题参考不进入模型；先完成预测再由离线评分器读取。

L3由程序规则确定，Qwen负责分组/家族和解释；因此本报告评的是组合工作流，不是“Qwen独立82类分类”或旧Notebook指标。未做独立语义groundedness评测、真实临床数据评测或模型微调。

## 4. 实际浏览器闭环

用户授权后，停止了原8911服务，先复制已关闭连接的SQLite文件为 `runtime/web/qc.before-m4-20260910.sqlite3`，完整性检查为ok。原16条案例、5次运行、4条复核记录保留；升级后的对比没有发现旧案例/运行/复核缺失或变化。未重启Ollama或旧Jupyter，原账户密码未改。

使用原reviewer登录后，重新打开用户的30分钟练习 `SYN-WEB-6785d22e`：仍显示原M3运行、30≥30和原草稿，不重新计算或改写历史。

随后在网页真实运行 `DEMO-MULTI-01`：

- 网页任务：`521d7651-0208-48f8-9352-dcf06846ab79`。
- 模型运行：`d68bd1f8-b49f-449a-91d3-79dd3168f315`，17.9秒。
- 使用网页已发布的规则快照 `aa5251e2-b01f-4775-a58d-45c8339cbb08`，保留用户此前发布的额外PK规则。评测则固定使用M1文件知识，两者不混淆。
- 实际得到I1：PK27<30、中风险；I2：AE漏记、高风险。总览两项、零未决、高优先级。
- I1草稿末尾仍有“符合要求”的歧义；I2草稿把“漏记”写成“漏中”。两者均通过编辑人工意见修正，原始草稿保留。
- 保存I1后，后台运行列表显示revision1、提醒pending；I2没有被自动关闭。
- 保存I2后，显示revision2、提醒closed；重新打开并展开历史，I1/I2意见分别存在。
- 操作理由明确写“浏览器自动化验收，非临床专业复核”，不冒充医生或专业质控签字。

实际检查了页面截图、评测页18条结果和六类支持数。按Sites构建/预览流程验证原有本地网页，未创建或发布云站点。评测页只读取文件，不再次调用模型或把评测记录混入网页复核队列。

## 5. 软件测试与构建

`.venv/bin/python -m unittest discover -s tests -q`：**122项通过**。其中原M1–M3的100项继续通过，M4新增计算、身份、分组、评分、逐项复核和评测API检查。

覆盖六类明确输入、边界、缺失不能当0、授权终点不包含、EDC对齐、不同主体/非法编号/否定/未解析尾句、全片段覆盖、family内部group、跨项引用拒绝、聚合未决、高风险、完整评测分母、整体状态计分、逐项关闭、无分项失败只能退回，以及评测接口登录保护和只读行为。

这些测试用确定的路由/替身验证软件，不拿通过数当模型准确率。TypeScript检查和Vite生产构建通过；依赖沿用M3锁文件，没有新增模型下载。测试适配器的Starlette/httpx弃用提示仍在，未造成失败。

## 6. 尚未完成的目标

通用自由文本事实抽取、其他五类规则网页发布、完整规则停用/更正生命周期、解释语义自动判真、真实医院评测与生产安全认证均未完成。M5的正式测试答卷、可复现交付/备份恢复和演示稿也仍是下一阶段，不能因本页有分数就声称全部测试要求已交付。

学习入口：[M4先做一个访视案例](M4_START_HERE_ZH.md)。
