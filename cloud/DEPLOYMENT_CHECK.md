# 云端部署验证

## 混合检索 v2 · 2026-09-14

已部署版本：`5ea6d1ac-6cf7-47b3-b576-8b25f2707245`。沿用原Worker、D1数据库和域名，没有数据库迁移、历史结果覆盖、套餐升级或付费回退。

公网 `/api/health` 已返回 `version=cloud-rag-v2`、`retrieval_version=cloud-retrieval-v2`，以及Qwen、`@cf/baai/bge-m3`、`@cf/baai/bge-reranker-base` 三个模型标识。首页已提供新的检索比较组件资源。以下为部署后真实HTTP测试，不是浏览器点击验收或临床准确率评测：

| 输入 | 保存结果 | 检索 / Qwen调用 | 服务端分析时间 | 复核保存重开 |
|---|---|---:|---:|---|
| QC002另存副本：27分钟PK | 计算27 < 30、L3命中；解释引用不合规，`partial_review_required` | 2 / 2 | 2.757秒 | 通过 |
| `DEMO-MULTI-01` | PK与AE两个问题，高风险保留，`proposed_findings` | 2 / 2 | 4.539秒 | 通过 |
| `DEMO-BOUNDARY-01`：30分钟 | `no_finding_for_checked_rule` | 2 / 2 | 2.845秒 | 通过 |

三条均真实完成一次向量批处理、一次交叉编码器重排、两次Qwen调用。四种排名、6条适用规则和实际送入Qwen的选中规则均核对成功。27分钟案例的Qwen草稿使用了没有方括号的引用编号，未通过原有引用校验；程序初判保留，解释未被标记为有效，也未重试以替换这次失败。

另验证了：无会话拒绝、CSRF拒绝、访客记录隔离、运行/导出/复核跨访客拒绝、重复请求不再调用模型、复核追加且不覆盖AI初判、公开规则只读。完成167项项目软件测试、6项原云端测试、10项新增检索集成测试，以及TypeScript检查、前端构建和部署dry-run。

单独的18条合成检索查询评测与这里3条端到端检查不是同一组测试；指标及局限见[混合检索说明](../docs/RETRIEVAL_V2_ZH.md)。三条线上样本的耗时不代表最大输入或并发性能；一次解释失败也没有从验收记录中排除。

本机直接域名访问先发生TLS失败，尚未创建会话或调用模型；之后通过Cloudflare官方DNS-over-HTTPS核实地址，仅为测试请求指定解析地址并保留证书验证，线上检查完成。没有修改系统DNS、hosts、代理、防火墙或本机端口。浏览器自动化权限未开放，因此未宣称完成逐按钮视觉验收。

## 原云端版本 · 2026-09-11

地址：<https://clinical-qc-public-demo.pqjspy.workers.dev>

该次已验证版本：`b7ad8a70-662d-454b-b5d6-f216c418a8f6`（历史记录，现已由上方v2替代）

- Worker：`clinical-qc-public-demo`
- D1：`clinical-qc-public-demo-db`，三条迁移完成。
- 模型：`@cf/qwen/qwen3-30b-a3b-fp8`，通过 Workers AI 绑定真实调用。
- 本地原应用、账号、历史及规则源文件未修改；云端无真实患者资料。
- 未升级 Workers Free、未添加付款方式、未接入付费回退。

## 最终公网实测

以下是软件演示验收，不是临床数据准确率评测。

| 输入 | 程序结果 | 问题数 | 分析时间 | 真实模型调用 | 复核保存重开 |
|---|---|---:|---:|---:|---|
| 27 分钟 PK（QC002 的另存副本） | `proposed_findings`；27 < 30 | 1 | 2.961 秒 | 2 | 通过 |
| `DEMO-MULTI-01` | `proposed_findings`；PK + AE，高风险保留 | 2 | 4.909 秒 | 2 | 通过 |
| `DEMO-BOUNDARY-01` | `no_finding_for_checked_rule`；恰好 30 分钟 | 1 | 1.717 秒 | 2 | 通过 |

三次分析的引用结构校验均通过。模型返回的 neuron 用量合计约 **35.507**。时间为服务端分析过程，不含打开页面、建立会话或人工操作耗时，也不是性能保证。

另外通过：

- 无会话访问拒绝；错误 CSRF 拒绝。
- 两个访客的另存记录相互隔离；外部访客读取运行、导出和写复核均返回 404。
- 重复分析请求键返回原运行，没有额外模型调用。
- 重复复核请求键返回原版本，不重复保存。
- 人工修改前后 AI 结果 JSON 和 SHA-256 相同。
- 公开规则发布接口不存在；规则库返回只读标识。
- 静态首页 HTTPS 200，安全响应头正常。
- 6 个离线软件测试组通过，包含 12 个合成输入、原规则程序哈希一致性、配额与复核隔离。

## 已知限制

当前本机 DNS 将 Workers 域名错误解析到其他地址。官方 Cloudflare DNS 查询和保留 TLS 校验的定向解析请求均证明该网站可访问；没有修改系统 DNS、hosts 或关闭证书校验。相同网络下的浏览器仍可能打不开，需单独解决解析问题。

本地预览代理的 AI 连接曾失败，公网绑定已实测成功。第一次公网解释的引用不合规时被正确拦截；随后保留原始草稿并加入严格的分组引用格式兼容，最终三例全部通过。

未执行浏览器逐按钮视觉验收，未验证最大输入/最大并发下的持续 CPU 余量；上述样本未出现资源超限。只报告真实完成的编译、软件与 HTTP 测试，不据此推断真实医院准确率。
